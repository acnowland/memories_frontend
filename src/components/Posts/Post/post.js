import React from "react";
import useStyles from "./style";

const Post = () => {
  const classes = useStyles();
  return (
    <div>
      <h1>Single Post</h1>
    </div>
  );
};

export default Post;
